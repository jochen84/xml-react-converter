Do "npm install" in root folder.
Then do "npm install" in /client folder.
Go to root folder and run "npm run dev" to start both server and app.

Choosing a file will move it to /assets/uploads and overwrite the file "konvertera.txt".
Convert will take that file and convert it and place a copy in /assets/exports and overwrite "konverterad.xml".
